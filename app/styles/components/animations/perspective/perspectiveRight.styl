@keyframes perspectiveRight
	0%
		transform-origin: 100% 0
		transform: perspective(em(800px)) rotateY(0deg)
	100%
		transform-origin: 100% 0
		transform: perspective(em(800px)) rotateY(180deg)

generateAnimationPlaceholder(perspectiveRight)
