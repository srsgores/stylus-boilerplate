@keyframes shake
	0%, 100%
		transform: translateX(0)

	10%, 30%, 50%, 70%, 90%
		transform: translateX(em(-10px))

	20%, 40%, 60%, 80%
		transform: translateX(em(10px))

generateAnimationPlaceholder(shake)
