@keyframes bounceInLeft
	0%
		opacity: 0
		transform: translateX(em(-2000px))

	60%
		opacity: 1
		transform: translateX(em(30px))

	80%
		transform: translateX(em(-10px))

	100%
		transform: translateX(0)

generateAnimationPlaceholder(bounceInLeft)
